// Test-only stub for the `server-only` package. In the real app this module
// throws when imported from a Client Component; under Vitest we just want the
// pure helpers alongside it (e.g. normalizeVendorName) to be importable.
export {};
